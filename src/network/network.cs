using System;
using System.Net;
using System.Net.NetworkInformation;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace CipherModule.Network
{
    class network
    {

        internal string ping(string ipaddr)
        {
            return "";
        }

        internal string ping(IPAddress ipaddr)
        {
            return "";
        }


        internal class regular
        {
            public static bool isCheck(string ipaddr)
            {
                return false;
            }

            public static bool isCheck(IPAddress ipaddr)
            {
                return false;
            }
        }
    }
}
